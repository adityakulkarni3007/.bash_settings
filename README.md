Just enter the following commands to add the bash settings to your bashrc: 

    vim ~/.bashrc

Add the following line to the end of your bashrc, then save and quit:

    source ~/.bashsettings/.custom_bash

